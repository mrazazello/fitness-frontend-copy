export const defaultPageSize = 10;

export const phoneRegExp = /^\+\d{11}$/;
export const slugRegExp = /^[A-Za-z0-9]+$/;
export const promocodeRegExp = /^[A-Za-zА-Яа-я0-9-]+$/;

export const emailRegExp = /^[\w-.]+@([\w-]+\.)+[\w-]{2,4}$/;

export const defaultBackendDateFormat = "YYYY-MM-DD";

export const dateFormatFull = "DD.MM.YYYY HH:MM";
export const dateFormatDate = "DD.MM.YYYY";
